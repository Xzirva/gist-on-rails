source 'https://rubygems.org'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.2.5'
# Use mysql as the database for Active Record
gem 'mysql2'
# Use SCSS for stylesheets
gem 'sass-rails', '~> 5.0'
# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'
# Use CoffeeScript for .coffee assets and views
gem 'coffee-rails', '~> 4.1.0'
# bundle install
gem 'bundler'
# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'turbolinks'
# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 2.0'
# bundle exec rake doc:rails generates the API under doc/api.
gem 'sdoc', '~> 0.4.0', group: :doc

# Bootstrap as CSS framework
gem 'bootstrap-sass', '~> 3.3.6'
gem 'bootstrap-datepicker-rails'
gem 'bootstrap3-datetimepicker-rails'
gem 'bootstrap-select-rails'

# Icons
gem 'font-awesome-rails'

# Comment generator for models
gem 'annotate'

#Display GitHub's style gist

# Javascript interpreter
gem 'execjs'

#To be able to use standard_api_ldap_client library
gem 'rest-client'

# Translation
gem 'rails-i18n'

#Heroku gems
gem 'rails_12factor', group: :production

group :development, :test do
  # Call 'byebug' anywhere in the code to stop execution and get a debugger console
  gem 'byebug'

  # Access an IRB console on exception pages or by using <%= console %> in views
  gem 'web-console', '~> 2.0'

  # Spring speeds up development by keeping your application running in the background. Read more: https://github.com/rails/spring
  gem 'spring'
end

group :development do
  ## Tools
  # Generate diagrams for the database
  gem 'railroady'

  # Dump the database to test/fixtures for easy fixtures generation :)
  gem 'fixtures_dumper'

  # Route cleaning tool
  gem 'traceroute'

  # Spots n+1 queries and unnecessary eager loading
  gem "bullet"

  # Profiler
  gem 'rack-mini-profiler'

  gem 'brakeman'
end
ruby "2.2.1"
